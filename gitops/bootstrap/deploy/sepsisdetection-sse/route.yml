apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: sepsisdetection-risk
spec:
  port:
    targetPort: http
  tls:
    termination: edge
  to:
    kind: Service
    name: sepsisdetection-risk
  wildcardPolicy: None
