---

sso_admin_credentials_secret: credential-rhsso

sso_realm_id: "{{ project_admin }}-sepsis"
sso_realm_template: "{{ resources_dir }}/realm.json"

sso_js_clientId: js

siteadmin_template: "{{ resources_dir }}/siteadmin-user.json"
siteadmin_add_role: "{{ resources_dir }}/add-role-to-siteadmin.json"

sso_config_configmap: sso-config
